import React from 'react';
import { Text, View } from 'react-native';

const Alert = () => {
    return (
        <View>
            <Text>Alert</Text>
        </View>
    );
};

export default Alert;
