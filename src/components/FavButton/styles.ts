import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    favoriteButton: {
        position: 'absolute',
        right: 0,
        padding: 10,
        bottom: 0,
        zIndex: 1
    },
    heartIcon: {
        width: 30,
        height: 30
    }
});
