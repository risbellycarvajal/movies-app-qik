import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    starsContainer: {
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'flex-start'
    },
    star: {
        marginRight: 4
    }
});
